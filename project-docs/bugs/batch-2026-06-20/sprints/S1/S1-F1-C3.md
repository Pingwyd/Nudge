# S1-F1-C3 — Verify Countdown Timer Fix

**PROMPT ID:** S1-F1-C3
**MODEL:** MiMo V2.5
**TYPE:** Integration-Test
**WHY THIS MODEL:** Requires reasoning about edge cases and verifying across all 3 code paths
**DEPENDS ON:** S1-F1-C2
**CONFIDENCE IN ROOT CAUSE:** Certain
**CONTEXT:** After C2 implements the fix, this chunk verifies correctness.

---

**PROMPT TO PASTE:**
"""
OBJECTIVE: Verify the countdown timer fix works correctly across all code paths.

REQUIREMENTS:
1. Read the modified `main_window.py` and confirm the 3 new `set_task_ref` calls are present in:
   - `_set_task_reminder()` (after `self.store.save()`)
   - `_set_task_reminder_at_time()` (after `self.store.save()`)
   - `_show_custom_reminder_dialog()` (after `self.store.save()`)

2. Verify the lookup pattern is correct:
   - Uses `self.task_row_widgets.get(id(task_ref))` (not `[]` which would KeyError)
   - Checks `if row is not None` before calling

3. Edge case analysis:
   - What if the task was just added and the widget hasn't been created yet? (Should be safe — `get` returns None)
   - What if the user sets a reminder from the context menu on a task that's currently being edited? (Should work — `set_task_ref` doesn't interfere with edit mode)
   - What if `reminderRepeat > 0` and the timer fires and reschedules? (The `reminderAt` is updated in `_check_task_reminders` — does that also need a `set_task_ref` call?)

4. Check `_check_task_reminders()` (line 3337-3363) — when a repeating reminder reschedules, does it update `task_ref["reminderAt"]`? If so, does the countdown need to be restarted?

TECH STACK & CONSTRAINTS:
- PyQt6, Python 3.11
- Verify by reading code, not running tests

EXPECTED OUTPUT:
- Confirmation that all 3 fixes are correctly placed
- Analysis of edge cases
- Whether `_check_task_reminders` also needs the fix (for repeating reminders)
"""

**ACCEPTANCE CRITERIA:**
- All 3 setter methods confirmed to have the fix
- Edge cases analyzed and documented
- Repeating reminder reschedule path analyzed
