# S1-F2-C3 — Verify Tray Icon Fix

**PROMPT ID:** S1-F2-C3
**MODEL:** MiMo V2.5
**TYPE:** Integration-Test
**WHY THIS MODEL:** Requires reasoning about window state transitions and platform behavior
**DEPENDS ON:** S1-F2-C2
**CONFIDENCE IN ROOT CAUSE:** Probable
**CONTEXT:** After C2 implements the tray fix, this chunk verifies correctness.

---

**PROMPT TO PASTE:**
"""
OBJECTIVE: Verify the tray icon fix works correctly and doesn't break existing flows.

REQUIREMENTS:
1. Read the modified `main_window.py` and verify:
   - Minimize button connects to `_minimize_to_tray` (not `showMinimized`)
   - `_minimize_to_tray` calls `self.hide()` and shows tray notification
   - `_show_from_tray` calls `self.show()` before `self.showNormal()`
   - `closeEvent` still hides to tray (unchanged)
   - `_quit_from_tray` still works (unchanged)

2. Trace the full lifecycle:
   - App starts → tray icon visible → window visible
   - User clicks minimize → window hidden, tray icon still visible
   - User clicks tray icon → window restores
   - User clicks close (X) → window hidden to tray
   - User clicks tray icon → window restores
   - User right-clicks tray → menu appears → "Quit" → app exits

3. Edge cases:
   - What happens if user clicks minimize twice rapidly?
   - What happens if user clicks tray icon while window is already visible?
   - What happens if `_show_from_tray` is called when window is minimized (not hidden)?

4. Verify no platform-specific code was introduced (no `sys.platform`, no `ctypes`, no `os.name` checks)

EXPECTED OUTPUT:
- All lifecycle steps verified as correct
- Edge cases analyzed
- Platform independence confirmed
"""

**ACCEPTANCE CRITERIA:**
- All tray interactions work: minimize, restore, close, quit
- No platform-specific code
- Edge cases handled gracefully
