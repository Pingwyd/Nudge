# S1-F1-C1 — Diagnose Countdown Timer Root Cause

**PROMPT ID:** S1-F1-C1
**MODEL:** MiMo V2.5
**TYPE:** Architecture-Reasoning
**WHY THIS MODEL:** Requires tracing signal flow across multiple setter paths to confirm the missing notification pattern
**DEPENDS ON:** none
**CONFIDENCE IN ROOT CAUSE:** Certain
**CONTEXT:** Countdown labels were added to TaskRowWidget in v1.9.0. `set_task_ref()` starts the timer, but the 3 reminder setter paths mutate the dict directly without notifying the widget.

---

**PROMPT TO PASTE:**
"""
OBJECTIVE: Confirm the root cause of countdown labels not appearing until app restart, and design the fix approach.

ROOT CAUSE (suspected): Three methods in MainWindow mutate `task_ref["reminderAt"]` directly but never call `set_task_ref()` on the corresponding TaskRowWidget:
1. `_set_task_reminder()` (line 3365)
2. `_set_task_reminder_at_time()` (line 3375)
3. `_show_custom_reminder_dialog()` (line 3627)

The widget's `_task_ref` points to the same dict object, so `_update_countdown()` would see the new value IF the timer were running — but `_start_countdown()` is never called after the mutation.

REQUIREMENTS:
- Trace each of the 3 setter paths and confirm they never notify the widget
- Confirm that `task_row_widgets[id(task_ref)]` can reliably look up the widget
- Design the minimal fix: call `row.set_task_ref(task_ref)` after each mutation
- Consider edge cases: what if the widget doesn't exist (task not rendered)?

TECH STACK & CONSTRAINTS:
- PyQt6, Python 3.11
- TaskRowWidget is keyed by `id(task)` in `self.task_row_widgets`
- `set_task_ref` already handles start/stop logic (checks `reminderAt` and `reminderFired`)
- No new signals or classes needed — just a method call

EXPECTED OUTPUT:
- Confirmation of root cause with line-number evidence
- Exact fix specification: which lines to modify, what code to add
- Edge case analysis
"""

**ACCEPTANCE CRITERIA:**
- Root cause confirmed with evidence from all 3 setter paths
- Fix approach specified as: add `row.set_task_ref(task_ref)` call after each mutation
- `task_row_widgets` lookup verified as reliable
