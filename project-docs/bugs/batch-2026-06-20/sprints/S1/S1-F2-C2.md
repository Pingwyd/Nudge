# S1-F2-C2 — Fix Tray Icon Behavior

**PROMPT ID:** S1-F2-C2
**MODEL:** DeepSeek V4 Flash
**TYPE:** Pure-Logic
**WHY THIS MODEL:** Mechanical fix once the diagnosis is clear — change method calls in 2-3 locations
**DEPENDS ON:** S1-F2-C1
**CONFIDENCE IN ROOT CAUSE:** Probable (confirmed by C1)
**CONTEXT:** After C1 diagnoses the tray issues, this chunk implements the fix.

---

**PROMPT TO PASTE:**
"""
OBJECTIVE: Fix minimize-to-tray and restore-from-tray to work properly on all platforms.

ROOT CAUSE (from C1 diagnosis):
- Minimize button calls `showMinimized()` which goes to taskbar, not tray
- Restore may fail because `showNormal()` after `hide()` doesn't always work on Windows

FIX REQUIREMENTS:

1. **Minimize button** — change from `showMinimized()` to hide-to-tray:
   - Replace `self.btn_minimize.clicked.connect(self.showMinimized)` with a call to the same hide logic used by `closeEvent`
   - The hide-to-tray logic is: `self.hide()` + show tray notification (once)

2. **Restore from tray** — make `_show_from_tray` more robust:
   - Use `self.show()` first (ensures visibility), then `self.showNormal()` (if minimized), then `self.activateWindow()` + `self.raise_()`
   - Alternative: just call `self.setVisible(True)` + `self.activateWindow()` + `self.raise_()`

3. **Verify close button** — `closeEvent` already hides to tray (confirmed working). No change needed.

4. **Verify Ctrl+M** — `_toggle_tray_visibility` already toggles. No change needed.

FILES TO MODIFY:
- `src/frontend/main_window.py`:
  - Line ~2204: change `self.btn_minimize.clicked.connect(self.showMinimized)` to connect to a new `_minimize_to_tray` method
  - Add `_minimize_to_tray` method: `self.hide()` + one-time tray notification
  - Lines 1913-1916: update `_show_from_tray` to use `self.show()` before `self.showNormal()`

TECH STACK & CONSTRAINTS:
- PyQt6, Python 3.11
- No ctypes, no platform-specific code
- Must preserve existing close-to-tray and quit-from-tray behavior
- `_tray_notified` flag prevents repeated notifications — reuse it

EXPECTED OUTPUT:
- Minimize button hides to tray (not taskbar)
- Tray icon left-click restores window reliably
- Close button still hides to tray
- Ctrl+M still toggles
"""

**ACCEPTANCE CRITERIA:**
- Minimize button hides to tray
- Tray icon click restores window
- No platform-specific code introduced
- Existing close/quit flows unchanged
