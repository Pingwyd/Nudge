# S1-F1-C2 — Implement Countdown Timer Fix

**PROMPT ID:** S1-F1-C2
**MODEL:** DeepSeek V4 Flash
**TYPE:** Pure-Logic
**WHY THIS MODEL:** Mechanical fix — add a 3-line lookup + method call at 3 locations. Pattern is identical across all 3 sites.
**DEPENDS ON:** S1-F1-C1
**CONFIDENCE IN ROOT CAUSE:** Certain (confirmed by C1)
**CONTEXT:** After C1 confirms the root cause, this chunk implements the fix.

---

**PROMPT TO PASTE:**
"""
OBJECTIVE: Fix countdown labels not appearing when reminders are set (without app restart).

ROOT CAUSE: `_set_task_reminder()`, `_set_task_reminder_at_time()`, and `_show_custom_reminder_dialog()` in `main_window.py` mutate `task_ref["reminderAt"]` but never call `set_task_ref()` on the corresponding TaskRowWidget to start the countdown timer.

REQUIREMENTS:
- After each reminder mutation in the 3 setter methods, look up the TaskRowWidget and call `set_task_ref(task_ref)`
- The lookup pattern is:
  ```python
  row = self.task_row_widgets.get(id(task_ref))
  if row is not None:
      row.set_task_ref(task_ref)
  ```
- Add this at the END of each setter method, AFTER the `self.store.save()` call
- Do NOT change any other logic in these methods

FILES TO MODIFY:
- `src/frontend/main_window.py`:
  - `_set_task_reminder()` — add after line 3373 (`self.store.save(self.tasks)`)
  - `_set_task_reminder_at_time()` — add after line 3386 (`self.store.save(self.tasks)`)
  - `_show_custom_reminder_dialog()` — add after line 3633 (`self.store.save(self.tasks)`)

TECH STACK & CONSTRAINTS:
- PyQt6, Python 3.11
- `self.task_row_widgets` is a dict mapping `id(task)` → `TaskRowWidget`
- `set_task_ref()` already handles start/stop logic internally
- No new imports needed

EXPECTED OUTPUT:
- 3 identical blocks added to 3 methods in `main_window.py`
- Each block: lookup widget by `id(task_ref)`, call `set_task_ref(task_ref)` if found
- No other changes to the methods

ASSUMPTION: `id(task_ref)` is stable and matches the key used in `task_row_widgets` — this is verified because `task_row_widgets[id(task)] = row` is set in `render_tasks()` and `_append_task_row_widget()`.
"""

**ACCEPTANCE CRITERIA:**
- All 3 setter methods now call `row.set_task_ref(task_ref)` after mutation
- No regressions in existing reminder functionality
- Countdown label appears immediately when a reminder is set
