# S2-F1-C1 — Smart Undo Toast Positioning

**PROMPT ID:** S2-F1-C1
**MODEL:** DeepSeek V4 Flash
**TYPE:** Pure-Logic
**WHY THIS MODEL:** Straightforward geometry calculation — check bounds, choose position
**DEPENDS ON:** none
**CONFIDENCE IN ROOT CAUSE:** Certain
**CONTEXT:** `_reposition()` uses fixed coordinates. Need to add bounds checking.

---

**PROMPT TO PASTE:**
"""
OBJECTIVE: Fix UndoToast positioning so it stays visible — bottom-left inside window if it fits, bottom-right outside window if it doesn't.

ROOT CAUSE: `_reposition()` at `main_window.py:497-502` does:
```python
self.move(10, parent.height() - self.height() - 10)
```
No bounds checking — toast can clip below the window.

REQUIREMENTS:
- Replace `_reposition()` with smart positioning logic:
  1. Calculate toast dimensions: `toast_w = self.width()`, `toast_h = self.height()`
  2. Calculate parent dimensions: `parent_w = parent.width()`, `parent_h = parent.height()`
  3. Check if toast fits at bottom-left inside parent:
     - Fits vertically: `toast_h + 10 <= parent_h`
     - Fits horizontally: `toast_w + 10 <= parent_w`
  4. If fits: `self.move(10, parent_h - toast_h - 10)` (bottom-left inside)
  5. If doesn't fit: `self.move(parent_w - toast_w - 10, parent_h - toast_h - 10)` (bottom-right inside, but shifted right as far as possible — may be partially outside)
  6. Alternative for "outside window": use `parent.mapToGlobal QPoint` to place at screen coordinates below/right of the window

- Call `self.adjustSize()` before positioning (already done, keep it)
- Use `self.move()` for positioning (already used)

FILES TO MODIFY:
- `src/frontend/main_window.py` — replace `_reposition()` method (lines 497-502)

TECH STACK & CONSTRAINTS:
- PyQt6, Python 3.11
- `self.move()` uses parent-relative coordinates (since parent is set in __init__)
- `adjustSize()` must be called before measuring `self.width()`/`self.height()`
- Keep it simple — no animation, no screen-edge detection beyond basic bounds

EXPECTED OUTPUT:
- Updated `_reposition()` method with bounds checking
- Bottom-left preferred, bottom-right as fallback
"""

**ACCEPTANCE CRITERIA:**
- Toast appears at bottom-left when it fits inside the window
- Toast appears at bottom-right when it would clip
- No toast clipping outside visible area
