# S2-F3-C2 — Verify Dialog Toggle

**PROMPT ID:** S2-F3-C2
**MODEL:** MiMo V2.5
**TYPE:** Integration-Test
**WHY THIS MODEL:** Requires reasoning about signal flow and dialog lifecycle
**DEPENDS ON:** S2-F3-C1
**CONFIDENCE IN ROOT CAUSE:** Certain
**CONTEXT:** After C1 adds the closeEvent override, this chunk verifies toggle works for all dialogs.

---

**PROMPT TO PASTE:**
"""
OBJECTIVE: Verify the shortcut toggle works for all dialogs that use GlassPanelDialog.

REQUIREMENTS:
1. Read the updated `glass_panel_dialog.py` and verify `closeEvent` calls `self.reject()`

2. Verify toggle flow for HistoryDialog:
   - Ctrl+H → dialog opens → `_history_dialog` set → `finished` not yet emitted
   - Ctrl+H → `open_history()` → `_history_dialog is not None` → calls `.close()` → `closeEvent` → `reject()` → `done()` → `finished` emitted → `_on_side_dialog_closed()` → `_history_dialog = None`
   - Ctrl+H → `_history_dialog is None` → new dialog opens ✓

3. Verify SettingsDialog still works:
   - SettingsDialog overrides `closeEvent` and calls `self.reject()` — child takes precedence
   - Toggle should still work as before

4. Verify ExportDialog (if it inherits GlassPanelDialog):
   - Check if ExportDialog has its own close handling
   - If it inherits GlassPanelDialog, it now gets the toggle behavior too

5. Edge cases:
   - What if user clicks the X button on the dialog? (Same as close — should work)
   - What if user presses Escape? (GlassPanelDialog already handles Escape → close)
   - What if dialog has unsaved changes (Settings)? (SettingsDialog.closeEvent handles this)

EXPECTED OUTPUT:
- All dialog toggles verified as correct
- No regressions in Settings dialog unsaved-changes flow
- ExportDialog behavior documented
"""

**ACCEPTANCE CRITERIA:**
- Ctrl+H toggles History dialog open/closed
- Ctrl+, toggles Settings dialog open/closed (no regression)
- Escape closes dialogs correctly
- Unsaved changes prompt still works in Settings
