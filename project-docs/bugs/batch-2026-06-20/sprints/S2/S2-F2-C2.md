# S2-F2-C2 — Implement Position-Preserving Restore

**PROMPT ID:** S2-F2-C2
**MODEL:** DeepSeek V4 Flash
**TYPE:** Pure-Logic
**WHY THIS MODEL:** Mechanical implementation once the design is clear from C1
**DEPENDS ON:** S2-F2-C1
**CONFIDENCE IN ROOT CAUSE:** Certain
**CONTEXT:** After C1 designs the data flow, this chunk implements it.

---

**PROMPT TO PASTE:**
"""
OBJECTIVE: Implement position-preserving undo for task completion.

REQUIREMENTS (from C1 design):

1. In `archive_task()` (line ~3043), BEFORE removing the task from `self.tasks`:
   - Calculate and save the original index:
     ```python
     original_index = self.tasks.index(task_ref)
     ```
   - Store it on the archived task dict:
     ```python
     archived_task["_archivedFromIndex"] = original_index
     ```
   - This happens BEFORE `self.tasks.remove(task_ref)`

2. In `restore_task_from_history()` (line ~3088):
   - Read the saved index: `original_index = task_ref.get("_archivedFromIndex")`
   - Remove `_archivedFromIndex` from the restored task (don't persist it)
   - Flat mode (no groups):
     - If `original_index` is not None and `0 <= original_index <= len(self.tasks)`: insert at that index
     - Otherwise: append to end
   - Group mode:
     - Find the task's group: `group_id = restored_task.get("groupId", GENERAL_GROUP_ID)`
     - Get group tasks: `group_tasks = self._tasks_in_group(group_id)`
     - If `original_index` is within group bounds: insert at that position within the group
     - Rebuild the master list using `rebuild_tasks_preserving_groups()`
   - After inserting, call `render_tasks()` (or just add the widget at the right position)

3. After restore, call `self.store.save(self.tasks)` and update the UI

FILES TO MODIFY:
- `src/frontend/main_window.py`:
  - `archive_task()` — save index before removal
  - `restore_task_from_history()` — insert at saved index instead of appending

TECH STACK & CONSTRAINTS:
- `self.tasks` is a flat list — index is position in that list
- `self._tasks_in_group(group_id)` returns filtered tasks for a group
- `rebuild_tasks_preserving_groups()` rebuilds the master list from group ordering
- After inserting, the row widget must be created and added to the correct layout position

EXPECTED OUTPUT:
- Modified `archive_task()` with index saving
- Modified `restore_task_from_history()` with position-preserving insert
- Both flat and group mode handled
"""

**ACCEPTANCE CRITERIA:**
- Task restores to original position in flat mode
- Task restores to original position in group mode
- Out-of-bounds index falls back to appending
- `_archivedFromIndex` is not persisted to history.json
