# S2-F1-C2 — Verify Toast Positioning

**PROMPT ID:** S2-F1-C2
**MODEL:** MiMo V2.5
**TYPE:** Integration-Test
**WHY THIS MODEL:** Requires reasoning about edge cases with window sizes and positioning
**DEPENDS ON:** S2-F1-C1
**CONFIDENCE IN ROOT CAUSE:** Certain
**CONTEXT:** After C1 implements the positioning fix, this chunk verifies edge cases.

---

**PROMPT TO PASTE:**
"""
OBJECTIVE: Verify the undo toast positioning works correctly across window sizes.

REQUIREMENTS:
1. Read the updated `_reposition()` method and verify the bounds logic

2. Edge cases to analyze:
   - Window is very small (e.g., 200x100) — toast should go to bottom-right
   - Window is maximized — toast should fit at bottom-left
   - Window is resized after toast appears — does it reposition?
   - Toast text is very long — does it wrap and resize properly?

3. Verify `adjustSize()` is called before measuring toast dimensions

4. Check that `_apply_theme()` still runs before `_reposition()` in `showEvent`

EXPECTED OUTPUT:
- Logic verified as correct for all window size scenarios
- Edge cases documented
- Any issues flagged
"""

**ACCEPTANCE CRITERIA:**
- All edge cases analyzed
- Positioning logic confirmed correct
