# S2-F2-C3 — Verify Position-Preserving Restore

**PROMPT ID:** S2-F2-C3
**MODEL:** MiMo V2.5
**TYPE:** Integration-Test
**WHY THIS MODEL:** Requires reasoning about group interactions and edge cases
**DEPENDS ON:** S2-F2-C2
**CONFIDENCE IN ROOT CAUSE:** Certain
**CONTEXT:** After C2 implements the fix, this chunk verifies correctness.

---

**PROMPT TO PASTE:**
"""
OBJECTIVE: Verify the position-preserving undo works correctly in all scenarios.

REQUIREMENTS:
1. Read the modified `archive_task()` and `restore_task_from_history()` methods

2. Flat mode verification:
   - Task at index 3 is completed → archived → undo → should reappear at index 3
   - Task at index 0 (first) is completed → undo → should reappear at index 0
   - Task at last index is completed → undo → should reappear at end

3. Group mode verification:
   - Task in Group A at position 2 is completed → undo → should reappear at position 2 in Group A
   - What if Group A had tasks added while the task was archived?
   - What if the task's group was deleted while archived?

4. Edge cases:
   - Two tasks completed in sequence, undo the second one first
   - Undo a task that was completed, then another task was completed and undone
   - What if `_archivedFromIndex` is missing (backward compatibility)?

5. Verify `_archivedFromIndex` is NOT saved to history.json (only in memory)

EXPECTED OUTPUT:
- All scenarios verified as correct
- Edge cases documented with expected behavior
- Backward compatibility confirmed (missing index = append to end)
"""

**ACCEPTANCE CRITERIA:**
- Flat mode: task restores to original position
- Group mode: task restores to original position within its group
- Edge cases handled gracefully
- No persistence of transient metadata
