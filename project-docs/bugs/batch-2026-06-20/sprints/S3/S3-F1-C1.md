# S3-F1-C1 — Fix Update Dialog Auto-Resize

**PROMPT ID:** S3-F1-C1
**MODEL:** DeepSeek V4 Flash
**TYPE:** Pure-Logic
**WHY THIS MODEL:** Single-line fix — add `adjustSize()` call
**DEPENDS ON:** none
**CONFIDENCE IN ROOT CAUSE:** Certain
**CONTEXT:** `DownloadDialog._on_finished` shows error text but doesn't resize the dialog.

---

**PROMPT TO PASTE:**
"""
OBJECTIVE: Fix the update dialog so it resizes to show the full error text when a download fails.

ROOT CAUSE: In `update_dialog.py`, `_on_finished` (around line 242-250) sets `self.error_text.setPlainText(detail)` and shows it, but never calls `self.adjustSize()`. The dialog stays at its progress-bar size.

REQUIREMENTS:
- After showing the error text and changing the button text, add:
  ```python
  self.adjustSize()
  ```
- This should be placed AFTER the error text is shown and the button is updated
- `adjustSize()` will resize the dialog to fit its contents, including the now-visible QTextEdit

FILES TO MODIFY:
- `src/frontend/update_dialog.py` — in `_on_finished` method, after showing error_text and updating button

TECH STACK & CONSTRAINTS:
- PyQt6, Python 3.11
- `adjustSize()` is a QWidget method that resizes to minimum size hint
- The QTextEdit has `ReadOnly=True` and contains the error text
- No other changes needed

EXPECTED OUTPUT:
- `self.adjustSize()` added after error text display in `_on_finished`
- Dialog resizes to show full error text on failure
"""

**ACCEPTANCE CRITERIA:**
- Dialog auto-resizes when error text is displayed
- Full error text is visible without scrolling (or with minimal scrolling for very long errors)
