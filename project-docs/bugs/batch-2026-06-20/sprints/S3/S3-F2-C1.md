# S3-F2-C1 — Restore Native Export File Dialog

**PROMPT ID:** S3-F2-C1
**MODEL:** DeepSeek V4 Flash
**TYPE:** Pure-Logic
**WHY THIS MODEL:** Single-flag removal — trivially mechanical
**DEPENDS ON:** none
**CONFIDENCE IN ROOT CAUSE:** Certain
**CONTEXT:** `export_service.py` forces Qt's themed dialog via `DontUseNativeDialog`.

---

**PROMPT TO PASTE:**
"""
OBJECTIVE: Restore the native Windows Explorer file dialog for export operations.

ROOT CAUSE: In `export_service.py:238-241`, `QFileDialog.getSaveFileName` is called with:
```python
options=QFileDialog.Option.DontUseNativeDialog
```
This forces Qt's styled file dialog instead of the native Windows Explorer dialog.

REQUIREMENTS:
- Remove the `options=QFileDialog.Option.DontUseNativeDialog` parameter from `QFileDialog.getSaveFileName` in `export_service.py`
- There are TWO calls to `QFileDialog.getSaveFileName` in the file:
  1. Line ~238 — the main save dialog
  2. Line ~240 (inside `_prompt_retry_export`) — the retry dialog
- Remove `DontUseNativeDialog` from BOTH calls
- When `options` is not passed, Qt defaults to the native dialog

FILES TO MODIFY:
- `src/backend/export_service.py` — remove `options=QFileDialog.Option.DontUseNativeDialog` from both `QFileDialog.getSaveFileName` calls

TECH STACK & CONSTRAINTS:
- PyQt6, Python 3.11
- `QFileDialog.getSaveFileName` without `options` uses the native OS dialog
- No other changes needed

EXPECTED OUTPUT:
- Both `QFileDialog.getSaveFileName` calls no longer pass `DontUseNativeDialog`
- Native Windows Explorer dialog appears for export
"""

**ACCEPTANCE CRITERIA:**
- Export file dialog shows native Windows Explorer view
- File save still works correctly
- Retry dialog also uses native view
