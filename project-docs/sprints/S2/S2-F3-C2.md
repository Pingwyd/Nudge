PROMPT ID: S2-F3-C2
MODEL: DeepSeek V4 Flash
TYPE: Pure-Logic
WHY THIS MODEL: Adding new QSS generator functions to theme.py is pure-logic — take color/radius tokens, return QSS strings. No I/O, no side effects, deterministic output.
DEPENDS ON: S2-F3-C1 (need to know which tokens were missing before adding generators)
CONTEXT: Nudge is a PyQt6 frameless liquid-glass desktop task widget. The theme system is in src/frontend/theme.py. It has DARK_THEME, LIGHT_THEME, OLED_THEME dicts with "colors" and "radii" keys. It has QSS generator functions like `scroll_bar_stylesheet()`, `chrome_button_stylesheet()`, `glass_overlap_stylesheet()`. S2-F3-C1 applied theme stylesheets to entry widgets in SettingsDialog but may have found that some widget types (QKeySequenceEdit, QListWidget, QComboBox) don't have dedicated generator functions. This chunk adds those generators if needed.

PROMPT TO PASTE:
"""
OBJECTIVE: Add QSS generator functions to theme.py for entry widget types that need dedicated styling.

REQUIREMENTS:
1. Read src/frontend/theme.py to understand the existing pattern for QSS generators.
2. Check if generators exist for these widget types:
   - QKeySequenceEdit
   - QListWidget
   - QComboBox
   - QSpinBox
3. If generators are MISSING for any of these, add them following the existing pattern:
   - Function name: `{widget_type}_stylesheet(theme: dict) -> str`
   - Takes a theme dict (from get_theme()), returns a QSS string.
   - Uses theme["colors"] and theme["radii"] tokens.
   - Example pattern from existing code:
     ```python
     def scroll_bar_stylesheet(theme: dict) -> str:
         c = theme["colors"]
         r = theme["radii"]
         return f"""
             QScrollBar {{ ... {c.get('scrollbar')} ... }}
         """
     ```
4. Add these generator functions:
   - `key_sequence_edit_stylesheet(theme: dict) -> str` — background, border, text color, border-radius for QKeySequenceEdit
   - `list_widget_stylesheet(theme: dict) -> str` — background, text, selection, border for QListWidget
   - `combobox_stylesheet(theme: dict) -> str` — background, text, border, dropdown for QComboBox
5. Place them near the existing generator functions (after scroll_bar_stylesheet or similar).
6. Do NOT modify any existing functions.
7. Do NOT modify main.py or any file other than theme.py.
8. Export the new functions so they can be imported.

TECH STACK & CONSTRAINTS:
- Python 3, PyQt6
- File: src/frontend/theme.py
- Existing generators: scroll_bar_stylesheet(), chrome_button_stylesheet(), glass_overlap_stylesheet(), menu_stylesheet(), etc.
- Theme dict structure: {"colors": {"text": "...", "input_bg": "...", "border": "...", ...}, "radii": {"input": 10, "small": 4, ...}}
- QSS must be valid PyQt6 QSS.
- Use f-strings with theme token access: c.get("token_name", "fallback")
- Follow existing code style (function naming, docstrings, spacing).

EXPECTED OUTPUT:
- New functions in theme.py: key_sequence_edit_stylesheet, list_widget_stylesheet, combobox_stylesheet.
- Each returns a valid QSS string using theme tokens.
- No other files modified.
- Functions are importable.
"""

ACCEPTANCE CRITERIA:
- theme.py has key_sequence_edit_stylesheet(theme) that returns valid QSS.
- theme.py has list_widget_stylesheet(theme) that returns valid QSS.
- theme.py has combobox_stylesheet(theme) that returns valid QSS.
- Each function uses theme["colors"] and theme["radii"] tokens.
- Functions are importable without errors.
- No changes to existing functions.
