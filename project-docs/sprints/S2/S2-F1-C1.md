PROMPT ID: S2-F1-C1
MODEL: DeepSeek V4 Flash
TYPE: Effectful-IO
WHY THIS MODEL: Refactoring a button handler into a dispatcher with per-tab reset methods is pattern-based — find current handler, add index check, create reset methods for each tab. Well-defined mechanical work.
DEPENDS ON: S1 (all S1 chunks must be complete)
CONTEXT: Nudge is a PyQt6 frameless liquid-glass desktop task widget. The SettingsDialog class (src/frontend/main_window.py, line 750+) has a sidebar with tabs: General, Appearance, Shortcuts, Export, Advanced, Help. The "Reset to Defaults" button (line 1237) calls `_reset_shortcuts_to_defaults()` which only resets shortcut key sequences. The button should reset ONLY the currently active tab's values. The active tab is tracked by `self._stack` (QStackedWidget) — `self._stack.currentIndex()` returns the active tab index. The tab_names list is at line 1208: ["General", "Appearance", "Shortcuts", "Export", "Advanced", "Help"]. Each tab is a QWidget with named widgets (self.startup_cb, self.theme_combo, self.text_size_slider, self.opacity_slider, self.history_shortcut_edit, etc.).

PROMPT TO PASTE:
"""
OBJECTIVE: Refactor the "Reset to Defaults" button in SettingsDialog to reset only the active tab's values.

REQUIREMENTS:
1. In SettingsDialog (src/frontend/main_window.py), rename `_reset_shortcuts_to_defaults` to `_reset_active_tab_to_defaults`.
2. The new method should check `self._stack.currentIndex()` to determine which tab is active.
3. Create per-tab reset methods:
   - `_reset_general_tab_defaults()`: reset startup_cb, lock_cb, pin_cb, always_on_top_cb, check_updates_cb, boot_notification_cb to their default values (from the state_manager defaults or hardcoded: False for startup/lock/pin/always-on-top, True for check-updates, True for boot-notification).
   - `_reset_appearance_tab_defaults()`: reset theme_combo to "dark", text_size_slider to 14, opacity_slider to 100.
   - `_reset_shortcuts_tab_defaults()`: move existing `_reset_shortcuts_to_defaults` logic here (reset all QKeySequenceEdit widgets to defaults).
   - `_reset_export_tab_defaults()`: reset export_format_combo to "txt", export_include_history_cb to False.
   - `_reset_advanced_tab_defaults()`: reset groups_enabled_cb to True.
   - `_reset_help_tab_defaults()`: no-op (help tab has no configurable settings).
4. After resetting, call `self._mark_dirty()` so the user can save or discard.
5. Update the button's clicked connection from `_reset_shortcuts_to_defaults` to `_reset_active_tab_to_defaults`.
6. Do NOT change the button text (it stays "Reset to Defaults").

TECH STACK & CONSTRAINTS:
- Python 3, PyQt6
- File: src/frontend/main_window.py
- SettingsDialog class starts at line 750
- `_stack` is a QStackedWidget (line 1210)
- `tab_names` is at line 1208: ["General", "Appearance", "Shortcuts", "Export", "Advanced", "Help"]
- Tab indices: 0=General, 1=Appearance, 2=Shortcuts, 3=Export, 4=Advanced, 5=Help
- Default values:
  - General: startOnBoot=False, positionLocked=False, pinnedToDesktop=False, alwaysOnTop=False, checkForUpdates=True, showBootNotification=True
  - Appearance: theme="dark", taskTextSize=14, opacity=100
  - Shortcuts: historyShortcut="Ctrl+H", settingsShortcut="Ctrl+,", pinShortcut="Ctrl+P", alwaysOnTopShortcut="Alt+T", toggleTrayShortcut="Ctrl+M", exportShortcut="Ctrl+E"
  - Export: format="txt", includeHistory=False
  - Advanced: groupsEnabled=True
- Do not modify any other class or method.
- Do not change the button's position or appearance.

EXPECTED OUTPUT:
- Modified SettingsDialog with _reset_active_tab_to_defaults dispatcher.
- Six per-tab reset methods (one per tab).
- Button connection updated.
- No other files modified.
"""

ACCEPTANCE CRITERIA:
- Open General tab → change settings → Reset → only General values reset.
- Open Appearance tab → change theme → Reset → only Appearance resets.
- Open Shortcuts tab → change shortcuts → Reset → only shortcuts reset.
- Open Export tab → change format → Reset → only Export resets.
- Other tabs' values remain unchanged after reset on any tab.
- After reset, _mark_dirty() is called (Save button becomes active).
