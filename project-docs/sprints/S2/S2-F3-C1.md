PROMPT ID: S2-F3-C1
MODEL: DeepSeek V4 Flash
TYPE: UI
WHY THIS MODEL: Applying theme stylesheets to existing widgets is pattern-based — read theme tokens, generate QSS strings, apply to widgets. Well-understood UI theming work.
DEPENDS ON: S2-F2-C1 (the new Reminders tab must exist before theming it)
CONTEXT: Nudge is a PyQt6 frameless liquid-glass desktop task widget. The SettingsDialog (src/frontend/main_window.py) has entry widgets that don't fully respect the active theme (especially OLED). The theme system is in src/frontend/theme.py with get_theme(theme_id) returning dicts with "colors" and "radii". Three themes exist: dark, light, oled. The widgets that need theming:

1. QKeySequenceEdit widgets in the Shortcuts tab — inside nestedPanel QFrame cards. The cards have objectName "nestedPanel" but may not have themed background/border.
2. QListWidget (self._task_reminder_list in the new Reminders tab) — needs themed background, text color, selection color.
3. QComboBox widgets (self.theme_combo in Appearance, self.export_format_combo in Export) — need themed background, text, dropdown arrow.
4. QSpinBox widgets (self.text_size_slider's label area, any spinboxes) — need themed background.
5. The nestedPanel cards themselves — need themed background and border matching the active theme.

PROMPT TO PASTE:
"""
OBJECTIVE: Apply theme-aware stylesheets to all entry widgets in SettingsDialog so they match Dark, Light, and OLED themes.

REQUIREMENTS:
1. Create a method `_apply_entry_widget_theming(self)` in SettingsDialog.
2. This method should:
   a. Get the current theme: `theme = get_theme(self._get_theme_id())`
   b. Extract colors: `c = theme["colors"]`, `r = theme["radii"]`
   c. Apply stylesheets to these widget groups:
      - All QKeySequenceEdit widgets: background, border, color, border-radius
      - self._task_reminder_list (QListWidget): background, color, selection-background-color, border
      - self.theme_combo (QComboBox): background, color, border, border-radius
      - self.export_format_combo (QComboBox): same as above
      - All nestedPanel QFrames in Shortcuts tab: background, border, border-radius
3. Use these theme tokens:
   - Background: c.get("input_bg") or c.get("tab_bg")
   - Border: c.get("border")
   - Text: c.get("text")
   - Selection: c.get("accent") with opacity
   - Border radius: r.get("input") or r.get("small")
4. Call _apply_entry_widget_theming() at the end of init_ui().
5. Also call it in save_changes() after applying the theme, so new theme takes effect.
6. Do NOT modify theme.py yet — this chunk only applies existing tokens. If tokens are missing, note them for S2-F3-C2.
7. Style ALL instances, not just the first.

TECH STACK & CONSTRAINTS:
- Python 3, PyQt6
- File: src/frontend/main_window.py
- Theme: src/frontend/theme.py — get_theme(theme_id) returns dict with "colors" and "radii"
- Theme IDs: "dark", "light", "oled"
- QSS syntax: "QWidget { property: value; }" or "QWidget#objectName { ... }"
- Widgets to theme:
  - QKeySequenceEdit: use class selector "QKeySequenceEdit { ... }"
  - QListWidget: "QListWidget { ... }" and "QListWidget::item:selected { ... }"
  - QComboBox: "QComboBox { ... }" and "QComboBox::drop-down { ... }"
  - QFrame with objectName "nestedPanel": "QFrame#nestedPanel { ... }"
- Do not modify theme.py in this chunk.
- Do not change widget layouts or positions.

EXPECTED OUTPUT:
- New _apply_entry_widget_theming() method in SettingsDialog.
- All entry widgets styled with theme tokens.
- Method called at end of init_ui() and in save_changes().
- No other files modified.
"""

ACCEPTANCE CRITERIA:
- Switch to OLED → QKeySequenceEdit widgets have OLED background/border.
- Switch to OLED → QListWidget has OLED background.
- Switch to OLED → QComboBox widgets have OLED styling.
- Switch to Light → all entry widgets have light-appropriate styling.
- Switch to Dark → all entry widgets have dark-appropriate styling.
- No invisible text or invisible borders in any theme.
- No layout changes — only stylesheets applied.
