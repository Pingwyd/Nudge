PROMPT ID: S3-F2-C1
MODEL: DeepSeek V4 Flash
TYPE: UI
WHY THIS MODEL: Changing a signal connection from single-click to double-click is a trivial, mechanical change — one line in history_row.py plus a hint text update. No judgment needed.
DEPENDS ON: none (independent of S3-F1-C1)
CONTEXT: Nudge is a PyQt6 frameless liquid-glass desktop task widget. HistoryEntryLabel (src/frontend/history_row.py, line 15+) is a QLabel that currently restores a history entry on mousePressEvent (single-click). It has self._on_click callback, cursor set to PointingHandCursor, and tooltip "Click to restore to the task list". HistoryRowWidget (line 34+) wraps HistoryEntryLabel with a Delete button. HistoryDialog (src/frontend/main_window.py, line 464+) creates HistoryRowWidget instances with on_restore callback. The hint label in HistoryDialog.init_ui says "Click any entry to restore it to the task list." (line 501).

PROMPT TO PASTE:
"""
OBJECTIVE: Change history restore from single-click to double-click.

REQUIREMENTS:
1. In HistoryEntryLabel (src/frontend/history_row.py), change mousePressEvent to mouseDoubleClickEvent:
   - Current code (line 26-31):
     ```python
     def mousePressEvent(self, event):
         if event.button() == Qt.MouseButton.LeftButton and self._on_click is not None:
             self._on_click()
             event.accept()
             return
         super().mousePressEvent(event)
     ```
   - Change to:
     ```python
     def mouseDoubleClickEvent(self, event):
         if event.button() == Qt.MouseButton.LeftButton and self._on_click is not None:
             self._on_click()
             event.accept()
             return
         super().mouseDoubleClickEvent(event)
     ```
2. Update the tooltip in __init__ (line 24) from:
   `self.setToolTip("Click to restore to the task list")`
   to:
   `self.setToolTip("Double-click to restore to the task list")`
3. In HistoryDialog.init_ui (src/frontend/main_window.py, line 501), update the hint label:
   - Current: `hint = QLabel("Click any entry to restore it to the task list.")`
   - Change to: `hint = QLabel("Double-click any entry to restore it to the task list.")`
4. Do NOT change any other behavior — hover cursor, selection, delete button all stay the same.
5. Do NOT modify any other files.

TECH STACK & CONSTRAINTS:
- Python 3, PyQt6
- Files: src/frontend/history_row.py, src/frontend/main_window.py
- HistoryEntryLabel extends QLabel
- mousePressEvent → mouseDoubleClickEvent is a standard Qt rename
- The Delete button in HistoryRowWidget uses clicked.connect (not affected)
- Do not change HistoryRowWidget, only HistoryEntryLabel

EXPECTED OUTPUT:
- Modified src/frontend/history_row.py: mouseDoubleClickEvent replaces mousePressEvent, tooltip updated.
- Modified src/frontend/main_window.py: hint label text updated.
- No other files modified.
"""

ACCEPTANCE CRITERIA:
- Single-click on history entry does NOT restore it.
- Double-click on history entry DOES restore it to the main task list.
- Tooltip shows "Double-click to restore to the task list".
- Hint label shows "Double-click any entry to restore it to the task list".
- Hover cursor still shows pointing hand.
- Delete button still works (single-click).
