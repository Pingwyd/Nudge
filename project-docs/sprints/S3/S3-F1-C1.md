PROMPT ID: S3-F1-C1
MODEL: MiMo V2.5
TYPE: Architecture-Reasoning
WHY THIS MODEL: Adding a per-widget countdown timer with lifecycle management requires judgment — timer creation, cancellation, theme styling, interaction with existing task row layout, and performance considerations with 75+ tasks. Not a simple pattern.
DEPENDS ON: S2 (all S2 chunks complete)
CONTEXT: Nudge is a PyQt6 frameless liquid-glass desktop task widget. TaskRowWidget (src/frontend/main_window.py, line 155+) represents one task row. It has: self.label (QLabel), self.editor (QLineEdit), self.edit_btn (QPushButton), self.content_stack (QStackedWidget), self._task_ref (set externally). The layout is: [indent?] [content_stack (label or editor)] [edit_btn]. The edit_btn is at the right edge with AlignBottom. Tasks have a "reminderAt" field (ISO datetime string) and "reminderFired" (bool). The theme system provides get_theme(theme_id) with colors like "accent" (#4fc3f7 for dark/oled, #007AFF for light). The countdown should appear above or near the Edit button, only when a reminder is pending.

PROMPT TO PASTE:
"""
OBJECTIVE: Add a countdown indicator above the Edit button in TaskRowWidget that shows remaining time until a task's reminder fires.

REQUIREMENTS:
1. In TaskRowWidget (src/frontend/main_window.py), add a new QLabel attribute `self._countdown_label` that shows remaining time.
2. The countdown label should be positioned above the Edit button (using a QVBoxLayout wrapping the edit_btn, or by placing it in the existing layout before the edit_btn).
3. Format: `Xh Ym` (e.g., `1h 23m`, `45m`, `5m 30s`). Use int hours and int minutes; show seconds only when under 1 minute (`30s`).
4. Add a QTimer `self._countdown_timer` with 1-second interval that calls `_update_countdown()`.
5. The timer should only be started if the task has a pending reminder: `task.get("reminderAt")` is set AND `task.get("reminderFired")` is False.
6. Add a method `set_task_ref(self, task_ref)` that:
   - Sets `self._task_ref = task_ref`
   - If task_ref has a pending reminder, starts the countdown timer.
   - If not, stops the timer and hides the label.
7. `_update_countdown()` should:
   - Parse task_ref["reminderAt"] as ISO datetime.
   - Calculate remaining = reminder_dt - datetime.now().
   - If remaining <= 0: stop timer, hide label, return.
   - Format as `Xh Ym` or `Ym` or `Zs`.
   - Set label text and show label.
8. Style the countdown label:
   - Font size: 10px (small, subtle).
   - Color: theme["colors"]["accent"] (blue in dark/oled, blue in light).
   - Background: transparent.
   - Position: above the Edit button, aligned right.
9. When the task is edited (commit_edit), re-check if reminder is still valid.
10. When the task is deleted, stop the timer.
11. Only create timers for tasks that have active reminders — no timer for tasks without reminders.
12. In the parent code that creates TaskRowWidget (likely _append_task_row_widget or render_tasks), call `row.set_task_ref(task)` after creating the row.

TECH STACK & CONSTRAINTS:
- Python 3, PyQt6
- File: src/frontend/main_window.py
- TaskRowWidget starts at line 155
- edit_btn is at line 221, added to layout at line 226 with AlignBottom
- Layout: QHBoxLayout with content_stack (expanding) and edit_btn (fixed)
- Theme: get_theme(theme_id) from src/frontend/theme.py
- datetime already imported at top of main_window.py
- QTimer already imported (used elsewhere in the file)
- Performance: only create timers for tasks with active reminders. With 75 tasks and maybe 5-10 with reminders, this is fine.
- The countdown label should NOT interfere with the existing ResponsiveTextRowHelper layout.

EXPECTED OUTPUT:
- Modified TaskRowWidget with:
  - self._countdown_label (QLabel)
  - self._countdown_timer (QTimer, 1-second interval)
  - set_task_ref(task_ref) method
  - _update_countdown() method
  - Proper cleanup on deletion/edit
- Parent code calls set_task_ref() after creating rows.
- No other files modified.
"""

ACCEPTANCE CRITERIA:
- Task with pending reminder shows countdown (e.g., `1h 23m`) above Edit button.
- Countdown updates every second.
- Countdown disappears when reminder fires.
- Task without reminder shows no countdown.
- Countdown matches theme accent color.
- No performance issues with 75+ tasks (timers only for active reminders).
- Edit button still works correctly.
- Layout is not broken — countdown sits above/near Edit without overlapping task text.
