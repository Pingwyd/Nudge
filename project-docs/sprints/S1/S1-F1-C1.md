PROMPT ID: S1-F1-C1
MODEL: MiMo V2.5
TYPE: Architecture-Reasoning
WHY THIS MODEL: Requires reasoning about Qt's repaint cycle, frameless window behavior on Windows, and tradeoffs between Qt batching vs Win32 ctypes approaches — judgment call, not pattern code.
DEPENDS ON: none
CONTEXT: Nudge is a PyQt6 frameless liquid-glass desktop task widget for Windows. GlassPanelDialog (src/frontend/glass_panel_dialog.py) is the base class for History and Settings dialogs. It handles drag via mousePressEvent/mouseMoveEvent calling self.move() on every mouse event. This causes stutter at high mouse speeds because Qt repaints the entire translucent frameless window on each move. FramelessChromeController (src/frontend/frameless_chrome.py) handles MainWindow's drag/resize separately and is NOT involved in GlassPanelDialog's drag.

PROMPT TO PASTE:
"""
OBJECTIVE: Analyze the stuttering drag in GlassPanelDialog and recommend the best fix approach for a Windows PyQt6 frameless translucent window.

CONTEXT:
- GlassPanelDialog.mouseMoveEvent (src/frontend/glass_panel_dialog.py:112-120) calls self.move(self.pos() + delta) on every mouse move event.
- The window uses Qt.WindowType.FramelessWindowHint | Qt.WindowType.Dialog with WA_TranslucentBackground.
- On Windows, each self.move() triggers a full repaint of the translucent window, causing visible stutter at high mouse speeds.
- The window also has overlap detection in moveEvent that calls _update_overlap_opacity().

The current code:
```python
def mouseMoveEvent(self, event):
    if (
        event.buttons() == Qt.MouseButton.LeftButton
        and self._drag_pos is not None
    ):
        self.move(
            self.pos() + event.globalPosition().toPoint() - self._drag_pos
        )
        self._drag_pos = event.globalPosition().toPoint()
        event.accept()
```

REQUIREMENTS:
1. Recommend the best approach to eliminate stutter during drag on Windows.
2. Consider these options:
   a. Delta batching: only call self.move() when accumulated delta exceeds a threshold (e.g., 2px).
   b. Deferred move: store the target position and use QTimer.singleShot(0) to apply it in the next event loop iteration, coalescing multiple move events.
   c. Win32 ctypes: use win32gui.SetWindowPos via ctypes to move the window, bypassing Qt's repaint cycle entirely.
   d. Qt attribute tricks: WA_DontCreateNativeAncestors, WA_NoSystemBackground, or other attributes to reduce overdraw.
3. For each approach, explain: pros, cons, risk of breaking overlap detection, risk of breaking resize handles.
4. Pick ONE approach and justify why it's the best fit for this specific app (frameless + translucent + overlap detection).

EXPECTED OUTPUT:
A clear recommendation with:
- Chosen approach and justification
- Pseudocode or description of the change needed in glass_panel_dialog.py
- Any risks or side effects to watch for
- Whether FramelessChromeController needs any changes (it shouldn't, but confirm)
"""

ACCEPTANCE CRITERIA:
- Response identifies the root cause (Qt repaints on every self.move() for translucent frameless windows).
- Recommends a specific approach with tradeoff analysis.
- Pseudocode is provided for the chosen approach.
- Confirms whether overlap detection in moveEvent will still work.
- Confirms FramelessChromeController is not affected.
