PROMPT ID: S1-F2-C1
MODEL: DeepSeek V4 Flash
TYPE: UI
WHY THIS MODEL: This is a UI refactor with clear requirements — convert a top-level widget to a child widget, adjust layout, apply theme. Pattern-based UI work with well-defined acceptance criteria.
DEPENDS ON: none
CONTEXT: Nudge is a PyQt6 frameless liquid-glass desktop task widget. The UndoToast class (src/frontend/main_window.py:379-462) is currently a top-level QFrame with WindowStaysOnTopHint | Tool flags. It positions itself at screen coordinates relative to the parent, which causes it to appear far from the app window. The main window class is MainWindow (src/frontend/main_window.py). It uses a central widget with a vertical layout containing: input bar, group selector, scroll area with task list. The theme system is in src/frontend/theme.py with get_theme() returning theme dicts with colors, radii, fonts. The toast should be converted to a child widget inside the main window, overlaying content.

PROMPT TO PASTE:
"""
OBJECTIVE: Convert UndoToast from a top-level window to a child widget inside MainWindow, fix its positioning and styling.

REQUIREMENTS:
1. Modify the UndoToast class in src/frontend/main_window.py (lines 379-462).
2. Remove window flags (FramelessWindowHint, WindowStaysOnTopHint, Tool) — it should be a plain QWidget child.
3. Remove WA_TranslucentBackground and WA_ShowWithoutActivating attributes.
4. The toast should be positioned as an overlay inside the main window's central widget, at the bottom, floating on top of the task list.
5. Use absolute positioning within the parent: position the toast at the bottom-left of the parent widget with a small margin (e.g., 10px from bottom, 10px from left).
6. Add setWordWrap(True) to the message label.
7. Constrain the toast's maximum width to 350px so it doesn't stretch across the entire window.
8. Apply liquid-glass theme styling using get_theme() from src/frontend/theme.py:
   - Background: theme["colors"]["glass_start"] or surface color
   - Border: theme["colors"]["border"]
   - Text: theme["colors"]["text"]
   - Undo button: theme["colors"]["accent"] for text color
   - Border radius: theme["radii"]["panel"] or 12px
9. The toast should be parented to the MainWindow instance (pass parent in __init__).
10. The toast should raise() itself to appear on top of other child widgets.
11. In _show_undo_toast, after creating the toast, call toast.raise_() and toast.show().
12. Remove the _position_near_parent method (no longer needed).
13. Remove the _apply_theme method's manual stylesheet if it can be replaced with theme tokens, or update it to use the correct theme function.
14. The _dismiss method should call self.deleteLater() as before.
15. The undo callback mechanism must still work (self._undo_callback).

TECH STACK & CONSTRAINTS:
- Python 3, PyQt6
- File: src/frontend/main_window.py
- UndoToast is at lines 379-462
- MainWindow class starts at line ~464 (after UndoToast)
- Theme: src/frontend/theme.py — get_theme(theme_id) returns dict with "colors" and "radii"
- normalize_theme_id() from theme.py resolves theme ID
- The toast is created in _show_undo_toast (line 2878) and shown via toast.show()
- The parent is always self (MainWindow instance)
- Existing toast stylesheet uses QFrame#undoToast selector

EXPECTED OUTPUT:
- Modified UndoToast class that is a child QWidget (no window flags).
- Toast appears inside the main window boundary at the bottom.
- Toast wraps long text, has max-width constraint.
- Toast uses theme tokens for styling.
- Toast auto-dismisses after 5 seconds.
- Undo button works.
- No changes to any other files.
"""

ACCEPTANCE CRITERIA:
- UndoToast is a QWidget child (not a top-level window).
- Toast appears inside the app window at the bottom, not at screen edge.
- Toast wraps long task names (word wrap enabled, max-width 350px).
- Toast matches liquid-glass theme in Dark, Light, and OLED modes.
- Toast auto-dismisses after 5 seconds.
- Undo button restores the completed task.
- No window flags (FramelessWindowHint, etc.) on the toast.
