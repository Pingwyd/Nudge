PROMPT ID: S1-F3-C1
MODEL: DeepSeek V4 Flash
TYPE: Effectful-IO
WHY THIS MODEL: Wiring an existing themed dialog into destructive action handlers is pattern-based — find the handlers, add a confirmation check before executing. Low judgment, clear pattern.
DEPENDS ON: none
CONTEXT: Nudge is a PyQt6 frameless liquid-glass desktop task widget. ThemedMessageDialog (src/frontend/themed_message_dialog.py) extends GlassPanelDialog and provides static methods: question(parent, title, message) → bool, warning(parent, title, message), information(parent, title, message). The question() method shows Yes/No buttons and returns True if Yes was clicked. It already uses the liquid-glass theme. The following destructive actions need confirmation dialogs:

1. Delete task: In MainWindow.show_task_context_menu (src/frontend/main_window.py, around line 2499), there's a "Delete" action that calls a delete handler.
2. Clear history: In HistoryDialog (src/frontend/main_window.py, line 464+), there's likely a "Clear All" button that clears history.
3. Clear selected reminders: In SettingsDialog (src/frontend/main_window.py, around line 1167), there's a "Clear Selected" button connected to _clear_selected_task_reminder.

PROMPT TO PASTE:
"""
OBJECTIVE: Add confirmation dialogs to all destructive actions in the app using the existing ThemedMessageDialog.

REQUIREMENTS:
1. Find all destructive action handlers in src/frontend/main_window.py:
   a. Delete task — search for "Delete" in show_task_context_menu or the delete handler method.
   b. Clear history — search for "Clear" in HistoryDialog or the clear history method.
   c. Clear selected reminders — search for _clear_selected_task_reminder or "Clear Selected" button.
2. Before each destructive action executes, show a ThemedMessageDialog.question() confirmation.
3. Use these confirmation messages:
   - Delete task: title="Delete Task", message="Are you sure you want to delete this task? This cannot be undone."
   - Clear history: title="Clear History", message="Are you sure you want to clear all history? This cannot be undone."
   - Clear selected reminders: title="Clear Reminders", message="Are you sure you want to clear the selected reminders?"
4. Only proceed with the action if the dialog returns True (user clicked Yes).
5. Import ThemedMessageDialog if not already imported: from src.frontend.themed_message_dialog import ThemedMessageDialog
6. Do NOT create a new dialog class — use the existing ThemedMessageDialog.question() static method.
7. Do NOT modify themed_message_dialog.py — it already works correctly.
8. If any other destructive actions are found during implementation (e.g., other "Delete" or "Clear" buttons), add confirmation to those as well.

TECH STACK & CONSTRAINTS:
- Python 3, PyQt6
- File: src/frontend/main_window.py (main file, ~3500 lines)
- ThemedMessageDialog is in src/frontend/themed_message_dialog.py
- ThemedMessageDialog.question(parent, title, message) returns True/False
- The parent parameter should be self (for MainWindow methods) or self.parent() (for dialog methods)
- Existing code uses Qt patterns: QAction.triggered.connect, QPushButton.clicked.connect
- Do not change the behavior of any action — only add a confirmation gate before it.

EXPECTED OUTPUT:
- Modified src/frontend/main_window.py with confirmation dialogs added to:
  1. Delete task handler
  2. Clear history handler
  3. Clear selected reminders handler
  4. Any other destructive actions found
- Each confirmation uses ThemedMessageDialog.question() with appropriate title and message.
- Action only proceeds if user clicks Yes.
- No other files modified.
"""

ACCEPTANCE CRITERIA:
- Delete task → confirmation dialog appears → Cancel aborts, Confirm deletes.
- Clear history → confirmation dialog appears → Cancel aborts, Confirm clears.
- Clear selected reminders → confirmation dialog appears → Cancel aborts, Confirm clears.
- All dialogs use ThemedMessageDialog (not QMessageBox).
- All dialogs match liquid-glass theme in Dark, Light, and OLED modes.
- No behavioral change to non-destructive actions.
