# C10: Cleanup Dead Code

**CHANGE TYPE:** Mechanical
**BREAKING RISK:** Low
**DEPENDS ON:** C3, C4, C5
**ASSIGNED MODEL:** ⚡ DeepSeek V4 Flash

---

**OBJECTIVE:** Remove dead code, unused constants, and stale references left over from the old reminder system.

**FILES:** `src/constants.py`, `src/frontend/main_window.py`, `scripts/mem_test.py`

**CHANGES:**

1. **`src/constants.py`:**
   - Remove `REMINDER_LIST_TEXT_MAX = 60` (line 157) — was never imported; hardcoded `[:60]` used instead
   - Keep `TASK_REMINDER_CHECK_MS` for now — removing it can be done in a separate cleanup pass (it's in constants.py and might be referenced elsewhere)

2. **`scripts/mem_test.py`:**
   - Remove `"reminderInterval": 30` from the mock state dict (line 50)
   - Remove any reference to `src.frontend.settings_dialog` (dead import)

3. **`src/frontend/main_window.py`:**
   - Verify no remaining references to `task.get("reminderAt")` in MainWindow or TaskRowWidget (should all be gone after C5/C7)
   - Verify no remaining references to `task.get("reminderFired")` or `task.get("reminderRepeat")`
   - Remove `TASK_REMINDER_CHECK_MS` from imports if it was imported

**ACCEPTANCE CRITERIA:**
- `REMINDER_LIST_TEXT_MAX` constant does not exist
- No `reminderInterval` in mem_test.py
- No `task.get("reminderAt")` references remain in main_window.py
- No `task.get("reminderFired")` references remain in main_window.py
- No `task.get("reminderRepeat")` references remain in main_window.py
- All syntax checks pass
